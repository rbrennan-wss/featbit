worker_processes 1;

load_module modules/debian.bullseye.ngx_curity_http_oauth_proxy_module_1.23.2.so;

events { worker_connections 1024; }
 
http {
 
    sendfile off;
    # proxy_buffering off;
    proxy_buffer_size           128k;
    proxy_buffers               4 256k;
    proxy_busy_buffers_size     256k;
    large_client_header_buffers 4 16k;
 
    server {
        
        listen 80 default_server;

        server_name _;

        location = /health {
            access_log off;
            add_header 'Content-Type' 'application/json';
            return 200 '{"status":"UP"}';
        }

        # location / {
        #     return 308 https://$host$request_uri;
        # }
        
    }

    server {
        # listen 443 ssl; # >V1.25 
        listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
	    # listen [::]:80 default_server;
        server_name featbit.example;
        ssl_certificate /certs/featbit-example.crt;
        ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
	    # server_name _;
	    # return 301 https://$host$request_uri;

        location /testauth {
            return 200 'auth success';
            add_header Content-Type text/plain;
        }

        location /spa/  {
            # return 200 'auth success';
            # add_header Content-Type text/plain;
            proxy_pass http://172.25.96.1:3000/;
            # proxy_redirect     off;
            # proxy_set_header   Host $host;
            # proxy_set_header   X-Real-IP $remote_addr;
            # proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_pass_request_headers      on;
        }

        location ~/oauth-agent(.*)$ {
            # proxy_pass         https://oauth-agent.featbit.local;
            # proxy_pass         http://172.25.96.1:8080/oauth-agent$1;
            proxy_pass         http://172.25.96.1:8082/oauth-agent$1;
            
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_request_headers      on;
            # proxy_set_header   X-Forwarded-Host $server_name;
        }

        location = /favicon.ico {
            log_not_found off;
        }

        

        location / {
    #         if ($request_method = 'OPTIONS') {
    #     add_header 'Access-Control-Allow-Origin' '*';
    #     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    #     #
    #     # Custom headers and headers various browsers *should* be OK with but aren't
    #     #
    #     add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
    #     #
    #     # Tell client that this pre-flight info is valid for 20 days
    #     #
    #     add_header 'Access-Control-Max-Age' 1728000;
    #     add_header 'Content-Type' 'text/plain; charset=utf-8';
    #     add_header 'Content-Length' 0;
    #     return 204;
    #  }

            # proxy_pass         http://172.25.96.1:8081;
            proxy_pass         http://172.25.96.1:4200; #used for local development of the ui
            # proxy_redirect     off;
            # proxy_set_header   Host $host;
            # proxy_set_header   X-Real-IP $remote_addr;
            # proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header   X-Forwarded-Host $server_name;
        }   
    }

    server {
        # listen 443 ssl; # >V1.25 
        listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
        server_name api.featbit.example;
        ssl_certificate /certs/featbit-example.crt;
        ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
        location / {
    #         if ($request_method = 'OPTIONS') {
    #     add_header 'Access-Control-Allow-Origin' '*';
    #     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    #     #
    #     # Custom headers and headers various browsers *should* be OK with but aren't
    #     #
    #     add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
    #     #
    #     # Tell client that this pre-flight info is valid for 20 days
    #     #
    #     add_header 'Access-Control-Max-Age' 1728000;
    #     add_header 'Content-Type' 'text/plain; charset=utf-8';
    #     add_header 'Content-Length' 0;
    #     return 204;
    #  }
            oauth_proxy on;
            oauth_proxy_cookie_name_prefix "featbit.example";
            oauth_proxy_encryption_key 4e4636356d65563e4c73233847503e3b21436e6f7629724950526f4b5e2e4e50;
            oauth_proxy_trusted_web_origin "https://featbit.example";
            oauth_proxy_cors_enabled on;

            proxy_pass http://172.25.96.1:5000;
            # proxy_pass http://127.0.0.1:5000;
            proxy_redirect     off;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

    server {
        # listen 443 ssl; # >V1.25 
        listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
        server_name eval.featbit.example;
        ssl_certificate /certs/featbit-example.crt;
        ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
        location / {
    #         if ($request_method = 'OPTIONS') {
    #     add_header 'Access-Control-Allow-Origin' '*';
    #     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    #     #
    #     # Custom headers and headers various browsers *should* be OK with but aren't
    #     #
    #     add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
    #     #
    #     # Tell client that this pre-flight info is valid for 20 days
    #     #
    #     add_header 'Access-Control-Max-Age' 1728000;
    #     add_header 'Content-Type' 'text/plain; charset=utf-8';
    #     add_header 'Content-Length' 0;
    #     return 204;
    #  }
            proxy_pass http://172.25.96.1:5100;
            proxy_redirect     off;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

    server {
        # listen 443 ssl; # >V1.25 
        listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
        server_name demo.featbit.example;
        ssl_certificate /certs/featbit-example.crt;
        ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
        location / {
    #         if ($request_method = 'OPTIONS') {
    #     add_header 'Access-Control-Allow-Origin' '*';
    #     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    #     #
    #     # Custom headers and headers various browsers *should* be OK with but aren't
    #     #
    #     add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
    #     #
    #     # Tell client that this pre-flight info is valid for 20 days
    #     #
    #     add_header 'Access-Control-Max-Age' 1728000;
    #     add_header 'Content-Type' 'text/plain; charset=utf-8';
    #     add_header 'Content-Length' 0;
    #     return 204;
    #  }
            proxy_pass http://172.25.96.1:5173;
            proxy_redirect     off;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

    # server {
    # listen 443 ssl; # >V1.25 
     #   listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
    #     server_name spa.featbit.example;
    #     ssl_certificate /certs/featbit-example.crt;
    #     ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
    #     location / {
    # #         if ($request_method = 'OPTIONS') {
    # #     add_header 'Access-Control-Allow-Origin' '*';
    # #     add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    # #     #
    # #     # Custom headers and headers various browsers *should* be OK with but aren't
    # #     #
    # #     add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
    # #     #
    # #     # Tell client that this pre-flight info is valid for 20 days
    # #     #
    # #     add_header 'Access-Control-Max-Age' 1728000;
    # #     add_header 'Content-Type' 'text/plain; charset=utf-8';
    # #     add_header 'Content-Length' 0;
    # #     return 204;
    # #  }
    #         proxy_pass https://172.25.96.1:3000;
    #         proxy_redirect     off;
    #         proxy_set_header   Host $host;
    #         proxy_set_header   X-Real-IP $remote_addr;
    #         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    #     }
    # }

    server {
        # listen 443 ssl; # >V1.25 
        listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
        server_name keycloak.featbit.example;
        ssl_certificate /certs/featbit-example.crt;
        ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
        location / {
            # proxy_pass https://keycloak.featbit.example:8443;
            proxy_pass https://172.25.96.1:8443;
            proxy_redirect     off;
            proxy_http_version 1.1;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header   Upgrade $http_upgrade;
            proxy_set_header   Connection keep-alive;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Forwarded-Host $server_name;
            # proxy_set_header   X-Forwarded-Server $host;
            # proxy_set_header   X-Forwarded-Port $server_port;
            proxy_pass_request_headers      on;
            # proxy_buffer_size           128k;
            # proxy_buffers               4 256k;
            # proxy_busy_buffers_size     256k;
            # large_client_header_buffers 4 16k;
            
        }
    }

    server {
        # listen 443 ssl; # >V1.25 
        # listen 8443 ssl; # >V1.25 
        listen 8443 ssl http2; # <V1.25
        listen 443 ssl http2; # <V1.25
        # http2 on; #>V1.25
        server_name ca.featbit.example;
        ssl_certificate /certs/featbit-example.crt;
        ssl_certificate_key /certs/featbit-example-pkcs-key.pem;
        location / {
            proxy_pass https://172.25.96.1:9000;
            proxy_redirect     off;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_pass_request_headers      on;
        }
    }

    
}
